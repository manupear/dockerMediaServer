- openweathermap:
    label: Naples, IT
    latitude: 40.8333
    longitude: 14.25
    units: metric # or imperial
    provider: openweathermap
    cache: 5 # Time in minutes to cache API responses, to stay within limits
    format: # optional, Intl.NumberFormat options
      maximumFractionDigits: 1

- greeting:
    text_size: xl
    text: America yaaaaaa

- search:
    provider: duckduckgo # google, duckduckgo, bing, baidu, brave or custom
    showSearchSuggestions: true # Optional, will show search suggestions. Defaults to false
    target: _blank # One of _self, _blank, _parent or _top

- glances:
    url: http://192.168.1.9:61208
    version: 4 # required only if running glances v4 or higher, defaults to 3
    cpu: true # optional, enabled by default, disable by setting to false
    mem: true # optional, enabled by default, disable by setting to false
    cputemp: true # disabled by default
    uptime: true # disabled by default
    disk: /dev/sda # disabled by default, use mount point of disk(s) in glances. Can also be a list (see below)
    diskUnits: bytes # optional, bytes (default) or bbytes. Only applies to disk
    expanded: true # show the expanded view
    label: HP SFF # optional



- datetime:
    text_size: xl
    format:
      dateStyle: long
      timeStyle: long
      hourCycle: h23